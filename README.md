This was done entirely by Layne Bumgarner because I would rather do things solo.

This is an extemely simple API calling program that allows you to determine the what gender a person likely is by their name. Simply input a valid name and the program will return its response.